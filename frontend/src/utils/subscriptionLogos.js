export const serviceLogoMappings = [
  { brand: "Netflix", domain: "netflix.com", keywords: ["netflix"], category: "Streaming", priceHints: [7.99, 14.99] },
  { brand: "Spotify", domain: "spotify.com", keywords: ["spotify"], category: "Music", priceHints: [11.12] },
  { brand: "Disney+", domain: "disneyplus.com", keywords: ["disney", "disney+", "disney plus"], category: "Streaming", priceHints: [5.99, 9.99] },
  { brand: "Amazon Prime", domain: "amazon.com", keywords: ["amazon prime", "prime video"], category: "Streaming", priceHints: [6.99] },
  { brand: "YouTube Premium", domain: "youtube.com", keywords: ["youtube premium", "youtube", "you tube", "yt"], category: "Streaming", priceHints: [12.99, 23.99] },
  { brand: "Canal+", domain: "canalplus.com", keywords: ["canal+", "canal plus", "canal"], category: "Streaming", priceHints: [19.99] },
  { brand: "Apple TV+", domain: "apple.com", keywords: ["apple tv", "apple tv+"], category: "Streaming", priceHints: [9.99] },
  { brand: "Paramount+", domain: "paramountplus.com", keywords: ["paramount", "paramount+"], category: "Streaming", priceHints: [7.99] },
  { brand: "Max", domain: "max.com", keywords: ["max", "hbo max"], category: "Streaming", priceHints: [9.99] },
  { brand: "Crunchyroll", domain: "crunchyroll.com", keywords: ["crunchyroll"], category: "Streaming", priceHints: [4.99, 6.49] },
  { brand: "Twitch", domain: "twitch.tv", keywords: ["twitch", "twitch sub"], category: "Streaming", priceHints: [4.99] },
  { brand: "Molotov", domain: "molotov.tv", keywords: ["molotov", "molotov tv"], category: "Streaming", priceHints: [6.99] },
  { brand: "DAZN", domain: "dazn.com", keywords: ["dazn", "sport streaming", "football", "boxing"], category: "Sports", priceHints: [14.99, 29.99] },
  { brand: "GMProno", domain: "gmprono.com", logoUrl: "/gmprono-favicon.png", keywords: ["gmprono", "gm prono", "prono foot", "pronostic football"], category: "Sports", priceHints: [14.99, 34.99] },
  { brand: "RMC Sport", domain: "rmcsport.tv", keywords: ["rmc sport", "rmc sports", "rmc", "champions league"], category: "Sports", priceHints: [19] },
  { brand: "beIN Sports", domain: "beinsports.com", keywords: ["bein sports", "bein sport", "bein", "be in sports"], category: "Sports", priceHints: [15] },
  { brand: "Eurosport", domain: "eurosport.fr", keywords: ["eurosport", "euro sport"], category: "Sports", priceHints: [6.99] },
  { brand: "L'Equipe", domain: "lequipe.fr", keywords: ["l'equipe", "lequipe", "l equipe", "journal sport"], category: "Sports", priceHints: [9.99] },
  { brand: "NBA League Pass", domain: "nba.com", keywords: ["nba league pass", "nba", "league pass"], category: "Sports", priceHints: [17.99] },
  { brand: "NFL Game Pass", domain: "nfl.com", keywords: ["nfl game pass", "nfl"], category: "Sports", priceHints: [13.99] },
  { brand: "F1 TV", domain: "formula1.com", keywords: ["f1 tv", "f1", "formula 1", "formule 1"], category: "Sports", priceHints: [7.99] },
  { brand: "UFC Fight Pass", domain: "ufcfightpass.com", keywords: ["ufc fight pass", "ufc", "mma"], category: "Sports", priceHints: [9.99] },
  { brand: "OCS", domain: "ocs.fr", keywords: ["ocs"], category: "Streaming", priceHints: [10.99] },
  { brand: "Deezer", domain: "deezer.com", keywords: ["deezer"], category: "Music", priceHints: [11.99] },
  { brand: "Apple Music", domain: "apple.com", keywords: ["apple music"], category: "Music", priceHints: [10.99] },
  { brand: "YouTube Music", domain: "music.youtube.com", keywords: ["youtube music", "yt music", "youtubemusic"], category: "Music", priceHints: [10.99] },
  { brand: "SoundCloud", domain: "soundcloud.com", keywords: ["soundcloud", "sound cloud"], category: "Music", priceHints: [5.99, 9.99] },
  { brand: "Basic-Fit", domain: "basic-fit.com", keywords: ["basicfit", "basic fit", "basic-fit", "basic"], category: "Fitness", priceHints: [24.99, 29.99] },
  { brand: "Fitness Park", domain: "fitnesspark.fr", keywords: ["fitness park"], category: "Fitness", priceHints: [29.95] },
  { brand: "Neoness", domain: "neoness.fr", keywords: ["neoness"], category: "Fitness", priceHints: [29.99] },
  { brand: "Keepcool", domain: "keepcool.fr", keywords: ["keep cool", "keepcool"], category: "Fitness", priceHints: [29.99] },
  { brand: "Adobe", domain: "adobe.com", keywords: ["adobe", "creative cloud", "adobe cc"], category: "Software", priceHints: [19.99, 59.99] },
  { brand: "Figma", domain: "figma.com", keywords: ["figma"], category: "Software", priceHints: [12] },
  { brand: "Canva", domain: "canva.com", keywords: ["canva"], category: "Software", priceHints: [11.99] },
  { brand: "Microsoft 365", domain: "microsoft.com", keywords: ["microsoft 365", "office 365", "office", "ms 365", "ms office"], category: "Software", priceHints: [7, 10] },
  { brand: "Notion", domain: "notion.so", keywords: ["notion"], category: "Software", priceHints: [9.5] },
  { brand: "Dropbox", domain: "dropbox.com", keywords: ["dropbox"], category: "Software", priceHints: [11.99] },
  { brand: "Google One", domain: "one.google.com", keywords: ["google one", "google storage"], category: "Software", priceHints: [1.99, 9.99] },
  { brand: "iCloud+", domain: "icloud.com", keywords: ["icloud", "icloud+"], category: "Software", priceHints: [0.99, 2.99, 9.99] },
  { brand: "ChatGPT", domain: "chatgpt.com", keywords: ["chatgpt", "openai", "chat gpt"], category: "Software", priceHints: [20] },
  { brand: "GitHub", domain: "github.com", keywords: ["github", "github copilot", "copilot"], category: "Software", priceHints: [4, 10] },
  { brand: "Slack", domain: "slack.com", keywords: ["slack"], category: "Software", priceHints: [8.75] },
  { brand: "Linear", domain: "linear.app", keywords: ["linear"], category: "Software", priceHints: [8] },
  { brand: "Vercel", domain: "vercel.com", keywords: ["vercel"], category: "Software", priceHints: [20] },
  { brand: "Cursor", domain: "cursor.com", keywords: ["cursor"], category: "Software", priceHints: [20] },
  { brand: "JetBrains", domain: "jetbrains.com", keywords: ["jetbrains", "webstorm", "phpstorm", "intellij"], category: "Software", priceHints: [16.9] },
  { brand: "1Password", domain: "1password.com", keywords: ["1password", "one password"], category: "Software", priceHints: [2.99, 4.99] },
  { brand: "Orange", domain: "orange.fr", keywords: ["orange"], category: "Other", priceHints: [29.99] },
  { brand: "Free", domain: "free.fr", keywords: ["free", "freebox"], category: "Other", priceHints: [19.99, 39.99] },
  { brand: "SFR", domain: "sfr.fr", keywords: ["sfr"], category: "Other", priceHints: [29.99] },
  { brand: "Bouygues Telecom", domain: "bouyguestelecom.fr", keywords: ["bouygues telecom", "bouygues", "bbox"], category: "Other", priceHints: [29.99] },
  { brand: "Uber", domain: "uber.com", keywords: ["uber eats", "uber eat", "uber"], category: "Other", priceHints: [5.99] },
  { brand: "Deliveroo", domain: "deliveroo.fr", keywords: ["deliveroo"], category: "Other", priceHints: [5.99] },
  { brand: "Amazon", domain: "amazon.com", keywords: ["amazon"], category: "Other", priceHints: [] },
  { brand: "Doctolib", domain: "doctolib.fr", keywords: ["doctolib"], category: "Other", priceHints: [] },
  { brand: "Qonto", domain: "qonto.com", keywords: ["qonto"], category: "Other", priceHints: [9, 29] },
  { brand: "Swile", domain: "swile.co", keywords: ["swile"], category: "Other", priceHints: [] },
  { brand: "Oney", domain: "oney.fr", keywords: ["oney", "oner"], category: "Other", priceHints: [] },
  { brand: "Revolut", domain: "revolut.com", keywords: ["revolut"], category: "Other", priceHints: [3.99, 9.99] },
  { brand: "Boursobank", domain: "boursobank.com", keywords: ["boursorama", "boursobank", "bourso bank"], category: "Other", priceHints: [] },
  { brand: "BNP Paribas", domain: "bnpparibas.fr", keywords: ["bnp paribas", "bnpparibas", "bnp"], category: "Other", priceHints: [] },
  { brand: "Credit Agricole", domain: "credit-agricole.fr", keywords: ["credit agricole", "credit-agricole", "ca banque"], category: "Other", priceHints: [] },
  { brand: "Societe Generale", domain: "societegenerale.fr", keywords: ["societe generale", "sg"], category: "Other", priceHints: [] },
  { brand: "MAIF", domain: "maif.fr", keywords: ["maif", "assurance maif"], category: "Insurance", priceHints: [12] },
  { brand: "AXA", domain: "axa.fr", keywords: ["axa", "axa assurance"], category: "Insurance", priceHints: [15] },
  { brand: "Macif", domain: "macif.fr", keywords: ["macif", "assurance macif"], category: "Insurance", priceHints: [12] },
  { brand: "Matmut", domain: "matmut.fr", keywords: ["matmut", "assurance matmut"], category: "Insurance", priceHints: [13] },
  { brand: "MMA", domain: "mma.fr", keywords: ["mma", "mma assurance"], category: "Insurance", priceHints: [14] },
  { brand: "MAAF", domain: "maaf.fr", keywords: ["maaf", "maaf assurance"], category: "Insurance", priceHints: [13] },
  { brand: "GMF", domain: "gmf.fr", keywords: ["gmf", "gmf assurance"], category: "Insurance", priceHints: [12] },
  { brand: "Allianz", domain: "allianz.fr", keywords: ["allianz", "allianz assurance"], category: "Insurance", priceHints: [16] },
  { brand: "Direct Assurance", domain: "direct-assurance.fr", keywords: ["direct assurance", "assurance auto"], category: "Insurance", priceHints: [10] },
  { brand: "Lovys", domain: "lovys.com", keywords: ["lovys", "assurance habitation"], category: "Insurance", priceHints: [9] },
  { brand: "Harmonie Mutuelle", domain: "harmonie-mutuelle.fr", keywords: ["harmonie mutuelle", "mutuelle sante"], category: "Insurance", priceHints: [24] },
  { brand: "BNK", domain: null, keywords: ["bnk"], category: "Other", priceHints: [] }
];

export const paymentMethodMappings = [
  { brand: "LCL", domain: "lcl.fr", keywords: ["lcl"] },
  { brand: "CIC", domain: "cic.fr", keywords: ["cic"] },
  { brand: "Revolut", domain: "revolut.com", keywords: ["revolut"] },
  { brand: "BNP Paribas", domain: "bnpparibas.fr", keywords: ["bnp", "bnp paribas", "bnpparibas"] },
  { brand: "Credit Agricole", domain: "credit-agricole.fr", keywords: ["credit agricole", "credit-agricole"] },
  { brand: "Societe Generale", domain: "societegenerale.fr", keywords: ["societe generale", "sg"] },
  { brand: "Boursobank", domain: "boursobank.com", keywords: ["boursobank", "boursorama"] },
  { brand: "Fortuneo", domain: "fortuneo.fr", keywords: ["fortuneo"] },
  { brand: "N26", domain: "n26.com", keywords: ["n26"] },
  { brand: "Nickel", domain: "nickel.eu", keywords: ["nickel"] },
  { brand: "Bunq", domain: "bunq.com", keywords: ["bunq"] },
  { brand: "Hello bank!", domain: "hellobank.fr", keywords: ["hello bank", "hellobank"] },
  { brand: "Monabanq", domain: "monabanq.com", keywords: ["monabanq"] },
  { brand: "La Banque Postale", domain: "labanquepostale.fr", keywords: ["banque postale", "la banque postale"] },
  { brand: "Banque Populaire", domain: "banquepopulaire.fr", keywords: ["banque populaire"] },
  { brand: "Caisse d'Epargne", domain: "caisse-epargne.fr", keywords: ["caisse d'epargne"] },
  { brand: "PayPal", domain: "paypal.com", keywords: ["paypal"] },
  { brand: "Apple Pay", domain: "apple.com", keywords: ["apple pay", "apple"] },
  { brand: "Google Pay", domain: "pay.google.com", keywords: ["google pay", "gpay"] },
  { brand: "Visa", domain: "visa.com", keywords: ["visa"] },
  { brand: "Mastercard", domain: "mastercard.com", keywords: ["mastercard", "master card"] },
  { brand: "American Express", domain: "americanexpress.com", keywords: ["american express", "amex"] },
  { brand: "Maestro", domain: "mastercard.com", keywords: ["maestro"] }
];

export function normalizeServiceName(value = "") {
  return value
    .toString()
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .toLowerCase()
    .replace(/[^\w+\s-]/g, " ")
    .replace(/\s+/g, " ")
    .trim();
}

export const suggestedServices = serviceLogoMappings
  .filter((service) => service.domain)
  .map(({ brand, domain, keywords, category, priceHints, logoUrl, fallbackUrl }) => ({
    name: brand,
    domain,
    category,
    priceHints,
    url: logoUrl,
    fallbackUrl,
    aliases: keywords.filter((keyword) => normalizeServiceName(keyword) !== normalizeServiceName(brand))
  }));

export const suggestedPaymentMethods = paymentMethodMappings.map(({ brand, domain, keywords }) => ({
  name: brand,
  domain,
  aliases: keywords.filter((keyword) => normalizeServiceName(keyword) !== normalizeServiceName(brand))
}));

function hasKeyword(normalizedName, keyword) {
  const normalizedKeyword = normalizeServiceName(keyword);
  if (!normalizedKeyword) return false;
  if (normalizedKeyword.length <= 3) {
    return new RegExp(`(^|\\s)${normalizedKeyword.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}(\\s|$)`).test(normalizedName);
  }
  return normalizedName.includes(normalizedKeyword);
}

function getBrandMatch(name) {
  const normalized = normalizeServiceName(name);
  if (!normalized) return null;

  return serviceLogoMappings
    .map((brand) => {
      const searchable = [brand.brand, ...brand.keywords].map(normalizeServiceName);
      return { brand, score: getSuggestionScore(searchable, normalized) };
    })
    .filter((item) => item.score > 0)
    .sort((a, b) => b.score - a.score || a.brand.brand.localeCompare(b.brand.brand))[0]?.brand ?? null;
}

function getPaymentMatch(name) {
  const normalized = normalizeServiceName(name);
  if (!normalized) return null;
  return paymentMethodMappings.find((method) => method.keywords.some((keyword) => hasKeyword(normalized, keyword))) ?? null;
}

function getWordStartsWith(value, normalized) {
  return value.split(" ").some((word) => word.startsWith(normalized));
}

function getCompactValue(value) {
  return value.replace(/\s+/g, "");
}

function getAcronym(value) {
  return value
    .split(" ")
    .filter(Boolean)
    .map((word) => word[0])
    .join("");
}

function getSuggestionScore(searchable, normalized) {
  const compactNormalized = getCompactValue(normalized);

  return searchable.reduce((bestScore, value, index) => {
    const compactValue = getCompactValue(value);
    const acronym = getAcronym(value);
    const canonicalBoost = index === 0 ? 4 : 0;
    let score = 0;

    if (value === normalized || compactValue === compactNormalized) {
      score = 100;
    } else if (
      value.startsWith(normalized) ||
      compactValue.startsWith(compactNormalized) ||
      normalized.startsWith(value) ||
      compactNormalized.startsWith(compactValue)
    ) {
      score = 86;
    } else if (getWordStartsWith(value, normalized) || acronym.startsWith(compactNormalized)) {
      score = 72;
    } else if (value.includes(normalized) || compactValue.includes(compactNormalized)) {
      score = 42;
    }

    return Math.max(bestScore, score + (score > 0 ? canonicalBoost : 0));
  }, 0);
}

function scoreSuggestions(items, normalized, key) {
  return items
    .map((item) => {
      const searchable = [item.name, ...item.aliases].map(normalizeServiceName);
      return { [key]: item, score: getSuggestionScore(searchable, normalized) };
    })
    .filter((item) => item.score > 0)
    .sort((a, b) => b.score - a.score || a[key].name.localeCompare(b[key].name));
}

export function getServiceSuggestions(query = "", limit = 6) {
  const normalized = normalizeServiceName(query);

  if (!normalized) {
    return suggestedServices.slice(0, limit);
  }

  return scoreSuggestions(suggestedServices, normalized, "service")
    .slice(0, limit)
    .map((item) => item.service);
}

export function getPaymentMethodSuggestions(query = "", limit = 8) {
  const normalized = normalizeServiceName(query);

  if (!normalized) {
    return suggestedPaymentMethods.slice(0, limit);
  }

  return scoreSuggestions(suggestedPaymentMethods, normalized, "method")
    .slice(0, limit)
    .map((item) => item.method);
}

export function getServiceDomain(serviceName) {
  const match = getBrandMatch(serviceName);
  return match?.domain ?? null;
}

export function getServicePlanSuggestion(serviceName) {
  const match = getBrandMatch(serviceName);
  if (!match) return null;
  return {
    name: match.brand,
    category: match.category ?? null,
    priceHints: match.priceHints ?? [],
    defaultPrice: match.priceHints?.[0] ?? null
  };
}

function hashName(name = "") {
  return Array.from(name.toString()).reduce((hash, char) => {
    return (hash * 31 + char.charCodeAt(0)) >>> 0;
  }, 7);
}

export function getServiceFallback(name = "") {
  const hash = hashName(normalizeServiceName(name));
  const hue = hash % 360;
  return {
    initials: getSubscriptionInitial(name),
    style: {
      backgroundColor: `hsl(${hue} 88% 96%)`,
      color: `hsl(${hue} 72% 42%)`
    }
  };
}

function buildLogoPayload(match, name, fallbackLabel) {
  const fallback = getServiceFallback(name);

  if (!match?.domain) {
    return {
      brand: match?.brand ?? name?.toString().trim() ?? fallbackLabel,
      domain: null,
      hasLogo: false,
      url: null,
      fallbackUrl: null,
      ...fallback
    };
  }

  return {
    ...match,
    hasLogo: true,
    url: match.logoUrl ?? `https://img.logo.dev/${match.domain}?size=128&format=png`,
    fallbackUrl: match.fallbackUrl ?? `https://www.google.com/s2/favicons?sz=128&domain=${match.domain}`,
    ...fallback
  };
}

export function getServiceLogo(name) {
  return buildLogoPayload(getBrandMatch(name), name, "Service");
}

export function getPaymentMethodLogo(name) {
  return buildLogoPayload(getPaymentMatch(name), name, "Payment");
}

export function getSubscriptionInitial(name = "") {
  const trimmed = name.toString().trim();
  if (!trimmed) return "?";
  const words = normalizeServiceName(trimmed).split(" ").filter(Boolean);
  if (words.length > 1) return words.slice(0, 2).map((word) => word[0]).join("").toUpperCase();
  return words[0]?.[0]?.toUpperCase() ?? trimmed[0].toUpperCase();
}

export const getSubscriptionLogo = getServiceLogo;
